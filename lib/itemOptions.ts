// Shared item-field option lists — used by any flow that drafts/edits an
// `items` row (Add Item, and the "log my items" outfit-decompose flow) so the
// choices stay in sync with the items_category / occasions conventions.
export const CATEGORIES = ["top", "bottom", "dress", "outerwear", "shoes", "bag", "accessory"];
export const OCCASIONS = ["workwear", "casual", "going-out", "athletic", "lounge"];
export const PROVENANCES = ["thrifted", "retail", "gifted", "secondhand", "handmade"];

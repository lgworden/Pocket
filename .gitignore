node_modules
.next
.env
.env.local
*.log
.tools/
tsconfig.tsbuildinfo
public/uploads/*
!public/uploads/.gitkeep

# Local-only embedded-postgres dev helper (not a project dependency — install
# ad hoc with `npm install --no-save embedded-postgres` before running).
start-db.mjs
